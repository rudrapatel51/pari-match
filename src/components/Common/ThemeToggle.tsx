/**
 * ThemeToggle — DEPRECATED.
 *
 * Dark / light mode is now controlled exclusively by the `mode` field in the
 * GET /app/theme API response. Manual toggling has been removed.
 *
 * This file is kept as an empty stub to avoid breaking any lazy-loaded import
 * that may still reference it in another branch. It renders nothing.
 */
const ThemeToggle = () => null;
export default ThemeToggle;
